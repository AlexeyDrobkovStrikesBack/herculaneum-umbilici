# Everything the shipped scripts need, at the versions every number in this
# repository was produced or re-verified with in this environment. Nothing here
# uses a recent feature, so older releases are very likely fine; these are the
# ones we actually ran, not a guess.
#
# numpy + scipy alone are enough for the seven scripts that run on a bare clone:
#   axis_stats.py  count_wins.py  order_stat.py  stick_control.py
#   snapshot_recheck.py  axis_benefit.py  prior_1218.py
#   (prior_1218.py needs numpy alone unless you pass --figure;
#    fetch_sean.py and pitch_table.py need neither)
# stat_figures.py runs on a bare clone too and adds matplotlib, nothing else,
# and so does prior_1218.py --figure.
numpy==2.5.1
scipy==1.18.0

# Only for the scripts that read the per-scroll L3 slice PNGs, which are not in
# this repository: validate_axes.py, validate_bands.py, qc_gates.py,
# qc_sheet.py, finalize.py, calib_sean.py, and the --measure modes of
# stick_control.py and snapshot_recheck.py.
Pillow==12.3.0

# For the figure scripts: scripts/stat_figures.py, which draws the four
# statistics panels of README sections 6, 5, 2 and 1; scripts/prior_1218.py
# --figure, which draws panels/prior_1218_agreement.png; and
# scripts/calib_figure.py, which draws panels/calibration_summary.png. With this
# version the first five regenerate byte-identically on a bare clone (re-checked
# 2026-08-15). calibration_summary.png regenerates byte-identically too
# (md5 a1c597503489e7fa2f5f89a1799b5005) but only where
# qc/calibration_sean.json is present, and that file is deliberately not
# shipped — see .gitignore — so it does not regenerate from a clone.
matplotlib==3.11.1
